import { DisableCompletedDirective } from './disable-completed.directive';

describe('DisableCompletedDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableCompletedDirective();
    expect(directive).toBeTruthy();
  });
});
